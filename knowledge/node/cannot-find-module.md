# Node Cannot Find Module

Category: node_missing_module

A missing Node module usually means dependencies are not installed or the package is not declared.

Safe sequence:
1. Inspect package.json.
2. Check the package manager and lockfile.
3. Install the requested package only with user approval.
4. Re-run the application.
